# Emotion Detector

This project implements an Emotion Detection application using the Watson NLP library. The application detects emotions from text and returns the dominant emotion along with emotion scores.
